-- init
if not game:IsLoaded() then 
    game.Loaded:Wait()
end

if not newcclosure then getgenv().newcclosure = function(f) return f end end
if not checkcaller then getgenv().checkcaller = function() return false end end
if not hookmetamethod then getgenv().hookmetamethod = function() return function(...) end end end

if not syn or not protectgui then
    getgenv().protectgui = function() end
end

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

local Window = Library:CreateWindow({
	Title = "Waster ware",
	Footer = "by @Nexsue",
    Icon = 0,
	NotifySide = "Right",
	ShowCustomCursor = true,
})

local SilentAimSettings = {
    Enabled = false,
    
    TeamCheck = false,
    VisibleCheck = true, 
    TargetPart = "HumanoidRootPart",
    SilentAimMethod = "Raycast",
    
    FOVRadius = 130,
    FOVVisible = false,
    ShowSilentAimTarget = false, 
    ShowBulletTracers = true,
    ShowHitNotifications = true,
    
    ESPEnabled = false,
    SimpleESPEnabled = false,
    
    CustomCameraFOV = false,
    CameraFOVAmount = 100,
    
    FlyJumpEnabled = false,
    
    ZoomButtonEnabled = false,
    
    UwUHitSound = false,
    CuteOpenSound = true,
    WeaponSkinEnabled = false,
    DeathESPEnabled = true,
    SkyboxEnabled = false,
    MorningTime = false,
    AutoHempEnabled = false,
    
    NoRecoil = false,
    NoSpread = false,
    PotatoGraphics = false,
    NoGrass = false,
    
    CoordFinder = false,
    MarkerX = 800,
    MarkerY = 400,
    
    Check360Enabled = false,
    AntiAimEnabled = false,
    ThirdPersonEnabled = false,
    ThirdPersonDistance = 15,
    VflyEnabled = false,
    VflySpeed = 1,
    SpiderEnabled = false,
    SpiderSpeed = 50,
    
    HitboxExpander = false,
    HitboxType = "Head", -- "Head" or "Body"
    HitboxSize = 2,
    
    MouseHitPrediction = false,
    MouseHitPredictionAmount = 0.165,
    HitChance = 100,
    
    InventoryEspEnabled = true,
    BackpackViewEnabled = true,
    BackpackDraggable = true,
    BackpackTargetMode = "FOV Target", -- "FOV Target" or "Closest Player"
    InventoryFovRadius = 150,
    KillNotifyEnabled = false,
    DeleteSpikesEnabled = false,
    DesyncEnabled = false
}

getgenv().SilentAimSettings = SilentAimSettings

local Camera = workspace.CurrentCamera
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local GuiService = game:GetService("GuiService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

local GetChildren = game.GetChildren
local GetPlayers = Players.GetPlayers
local WorldToScreen = Camera.WorldToScreenPoint
local WorldToViewportPoint = Camera.WorldToViewportPoint
local GetPartsObscuringTarget = Camera.GetPartsObscuringTarget
local FindFirstChild = game.FindFirstChild
local RenderStepped = RunService.RenderStepped
local GuiInset = GuiService.GetGuiInset
local GetMouseLocation = UserInputService.GetMouseLocation

local resume = coroutine.resume 
local create = coroutine.create

local ValidTargetParts = {"Head", "HumanoidRootPart"}
local PredictionAmount = 0.165

-- [[ ADDITIONAL FEATURES LOGIC ]] --
local spikesConn = nil
local desyncT = {loc = CFrame.new()}
local prevLookVector = nil
local isSpinning = false
local spinThreshold = 15

local function removeAllSpikes()
    local count = 0
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj.Name == "Spikes" then 
            pcall(function() obj:Destroy() end)
            count = count + 1 
        end
    end
    if count > 0 then 
        Library:Notify({
            Title = "Spikes Removed",
            Description = "Удалено " .. tostring(count) .. " шипов",
            Time = 3,
        })
    end
end

local function toggleDeleteSpikes(en)
    SilentAimSettings.DeleteSpikesEnabled = en
    if en then
        removeAllSpikes()
        if spikesConn then pcall(function() spikesConn:Disconnect() end) end
        spikesConn = workspace.DescendantAdded:Connect(function(obj)
            if SilentAimSettings.DeleteSpikesEnabled and obj.Name == "Spikes" then 
                task.defer(function() pcall(function() obj:Destroy() end) end) 
            end
        end)
    else
        if spikesConn then pcall(function() spikesConn:Disconnect() end); spikesConn = nil end
    end
end

local function onPlayerDied(player)
    if SilentAimSettings.KillNotifyEnabled then
        Library:Notify({
            Title = "Notification",
            Description = player.DisplayName .. " (" .. player.Name .. ") умер!",
            Time = 3,
        })
    end
end

local function hookDeath(p)
    if p == LocalPlayer then return end
    local function hc(c) 
        local h = c:WaitForChild("Humanoid", 15)
        if h then h.Died:Connect(function() onPlayerDied(p) end) end 
    end
    if p.Character then task.spawn(hc, p.Character) end
    p.CharacterAdded:Connect(hc)
end

local function getOffsetCFrame()
    local ping = LocalPlayer:GetNetworkPing() * 1000
    if ping < 100 then return CFrame.new(0, 0, -2)
    elseif ping <= 170 then return CFrame.new(0, 0, -2.7)
    else return CFrame.new(0, 0, -3.7) end
end

-- [[ Desync GUI Button ]] --
local DesyncGui = Instance.new("ScreenGui")
DesyncGui.Name = "DesyncGUI"
DesyncGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
if getgenv().protectgui then getgenv().protectgui(DesyncGui) end
DesyncGui.Parent = game:GetService("CoreGui")
DesyncGui.Enabled = false

local DesyncButton = Instance.new("TextButton")
DesyncButton.Size = UDim2.new(0, 100, 0, 40)
DesyncButton.Position = UDim2.new(0.5, -50, 0.5, -20)
DesyncButton.BackgroundColor3 = Color3.fromRGB(30,30,30)
DesyncButton.Text = "Desync: OFF"
DesyncButton.TextColor3 = Color3.new(1,1,1)
DesyncButton.Font = Enum.Font.SourceSansBold
DesyncButton.TextSize = 18
DesyncButton.Active = true
DesyncButton.Draggable = true
DesyncButton.Parent = DesyncGui

Instance.new("UICorner", DesyncButton).CornerRadius = UDim.new(0, 8)
local Stroke = Instance.new("UIStroke", DesyncButton)
Stroke.Color = Color3.fromRGB(100, 100, 100)
Stroke.Thickness = 2

DesyncButton.MouseButton1Click:Connect(function()
    SilentAimSettings.DesyncEnabled = not SilentAimSettings.DesyncEnabled
    DesyncButton.Text = SilentAimSettings.DesyncEnabled and "Desync: ON" or "Desync: OFF"
end)

local function CreateTracer(Origin, EndPoint)
    if not Origin or not EndPoint then return end
    local Distance = (EndPoint - Origin).Magnitude
    if Distance < 0.1 then return end

    local Tracer = Instance.new("Part")
    Tracer.Name = "Tracer"
    Tracer.Anchored = true
    Tracer.CanCollide = false
    pcall(function() Tracer.CanQuery = false end)
    pcall(function() Tracer.CanTouch = false end)
    Tracer.Transparency = 0.3
    Tracer.Material = Enum.Material.Neon
    Tracer.Color = Color3.fromRGB(170, 0, 255)
    
    Tracer.Size = Vector3.new(0.1, 0.1, Distance)
    Tracer.CFrame = CFrame.lookAt(Origin, EndPoint) * CFrame.new(0, 0, -Distance / 2)
    
    pcall(function()
        Tracer.Parent = workspace.CurrentCamera or workspace.Terrain or workspace
    end)
    
    task.delay(5, function()
        pcall(function() Tracer:Destroy() end)
    end)
end

local lastHitNotify = 0

-- [[ STAFF LIST LOGIC ]] --
local StaffSettings = {
    Enabled = false,
    OnlyOnline = false,
    Roles = {
        ["Admin"] = true, ["Admin+"] = true, ["Cool coconut"] = true, 
        ["Developer"] = true, ["Secret agent"] = true, ["Lead developer"] = true, ["Bob"] = true
    }
}
local GroupId = 15631191
local ListPos = Vector2.new(24, math.floor(Camera.ViewportSize.Y * 0.2))
local IsDragging, DragOffset, Expanded = false, Vector2.new(0,0), false
local UI, MemberDrawings = {}, {}

local StaffColors = {
    Border = Color3.fromRGB(120, 120, 120),
    Background = Color3.fromRGB(24, 24, 24),
    Header = Color3.fromRGB(34, 36, 39),
    Text = Color3.fromRGB(130, 100, 255),
    Online = Color3.fromRGB(45, 220, 70),
    Offline = Color3.fromRGB(230, 60, 60)
}

local function UpdateStaffPositions()
    if not UI.border then return end
    local baseW, baseH, rowH = 292, 28, 18
    UI.bg.Position = ListPos
    UI.border.Position = ListPos
    UI.header.Position = ListPos
    UI.arrow.Position = ListPos + Vector2.new(baseW - 24, 5)
    UI.title.Position = ListPos + Vector2.new(12, 4)
    UI.colNameLbl.Position = ListPos + Vector2.new(10, baseH + 2)
    UI.colRoleLbl.Position = ListPos + Vector2.new(120, baseH + 2)
    UI.colStatusLbl.Position = ListPos + Vector2.new(210, baseH + 2)
    for i, m in ipairs(MemberDrawings) do
        local y = ListPos.Y + baseH + 2 + 14 + ((i-1)*rowH)
        m.name.Position = Vector2.new(ListPos.X + 10, y)
        m.role.Position = Vector2.new(ListPos.X + 120, y)
        m.status.Position = Vector2.new(ListPos.X + 210, y)
    end
    local totalH = baseH + 2 + 14 + (#MemberDrawings * rowH) + 10
    UI.bg.Size = Vector2.new(baseW, Expanded and totalH or baseH)
    UI.border.Size = Vector2.new(baseW, Expanded and totalH or baseH)
    UI.header.Size = Vector2.new(baseW, baseH)
end

local function UpdateStaffVisibility()
    if not UI.border then return end
    local v = StaffSettings.Enabled
    UI.bg.Visible, UI.border.Visible, UI.header.Visible, UI.arrow.Visible, UI.title.Visible = v, v, v, v, v
    UI.arrow.Text = Expanded and "v" or ">"
    local show = v and Expanded
    UI.colNameLbl.Visible, UI.colRoleLbl.Visible, UI.colStatusLbl.Visible = show, show, show
    for _, m in ipairs(MemberDrawings) do
        m.name.Visible, m.role.Visible, m.status.Visible = show, show, show
    end
end

local function RefreshStaffList(staff)
    for _, m in ipairs(MemberDrawings) do pcall(function() m.name:Remove() m.role:Remove() m.status:Remove() end) end
    MemberDrawings = {}
    local show = StaffSettings.Enabled and Expanded
    for _, s in ipairs(staff) do
        local n = Drawing.new("Text") n.Text = s.name; n.Size = 14; n.Color = StaffColors.Text; n.Visible = show
        local r = Drawing.new("Text") r.Text = s.rank; r.Size = 14; r.Color = StaffColors.Text; r.Visible = show
        local st = Drawing.new("Text") st.Text = s.online and "Online" or "Offline"; st.Size = 14; st.Color = s.online and StaffColors.Online or StaffColors.Offline; st.Visible = show
        table.insert(MemberDrawings, {name = n, role = r, status = st})
    end
    UpdateStaffPositions()
end

local function FetchStaff()
    local staff = {}
    local success, rRaw = pcall(function() return game:HttpGet("https://groups.roblox.com/v1/groups/"..GroupId.."/roles") end)
    if not success then return staff end
    local roles = HttpService:JSONDecode(rRaw).roles
    for _, role in ipairs(roles) do
        if StaffSettings.Roles[role.name] then
            local success2, uRaw = pcall(function() return game:HttpGet("https://groups.roblox.com/v1/groups/"..GroupId.."/roles/"..role.id.."/users?limit=100") end)
            if success2 then
                for _, user in ipairs(HttpService:JSONDecode(uRaw).data) do
                    table.insert(staff, {name = user.username, rank = role.name, userId = user.userId, online = Players:FindFirstChild(user.username) ~= nil})
                end
            end
        end
    end
    if StaffSettings.OnlyOnline then
        local filtered = {}
        for _, s in ipairs(staff) do if s.online then table.insert(filtered, s) end end
        staff = filtered
    end
    return staff
end

local function InitStaffUI()
    UI.border = Drawing.new("Square"); UI.border.Thickness = 2; UI.border.Filled = false; UI.border.Color = StaffColors.Border
    UI.bg = Drawing.new("Square"); UI.bg.Filled = true; UI.bg.Color = StaffColors.Background; UI.bg.Transparency = 0.9
    UI.header = Drawing.new("Square"); UI.header.Filled = true; UI.header.Color = StaffColors.Header
    UI.arrow = Drawing.new("Text"); UI.arrow.Size = 16; UI.arrow.Color = StaffColors.Text; UI.arrow.Outline = true
    UI.title = Drawing.new("Text"); UI.title.Text = "Staff List"; UI.title.Size = 16; UI.title.Color = StaffColors.Text; UI.title.Outline = true
    UI.colNameLbl = Drawing.new("Text"); UI.colNameLbl.Text = "Name"; UI.colNameLbl.Size = 14; UI.colNameLbl.Color = StaffColors.Text
    UI.colRoleLbl = Drawing.new("Text"); UI.colRoleLbl.Text = "Role"; UI.colRoleLbl.Size = 14; UI.colRoleLbl.Color = StaffColors.Text
    UI.colStatusLbl = Drawing.new("Text"); UI.colStatusLbl.Text = "Status"; UI.colStatusLbl.Size = 14; UI.colStatusLbl.Color = StaffColors.Text
    UpdateStaffVisibility()
    UpdateStaffPositions()
end

UserInputService.InputBegan:Connect(function(input)
    if not StaffSettings.Enabled or not UI.border then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        local mPos = UserInputService:GetMouseLocation()
        if mPos.X >= (ListPos.X + 256) and mPos.X <= (ListPos.X + 292) and mPos.Y >= ListPos.Y + 36 and mPos.Y <= (ListPos.Y + 64) then
            Expanded = not Expanded; UpdateStaffVisibility(); UpdateStaffPositions(); return
        end
        if mPos.X >= ListPos.X and mPos.X <= (ListPos.X + 256) and mPos.Y >= ListPos.Y + 36 and mPos.Y <= (ListPos.Y + 64) then
            IsDragging = true; DragOffset = mPos - ListPos
        end
    end
end)
UserInputService.InputEnded:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then IsDragging = false end end)
RunService.RenderStepped:Connect(function() if StaffSettings.Enabled and IsDragging then ListPos = UserInputService:GetMouseLocation() - DragOffset; UpdateStaffPositions() end end)

-- Moved loops to end

local function NotifyHit(targetName)
    if tick() - lastHitNotify > 1 then
        lastHitNotify = tick()
        Library:Notify({
           Title = "Hit!",
           Description = "Hit target: " .. tostring(targetName),
           Time = 2
        })
    end
end

local function CreateOverlayGUI(name)
    local gui = Instance.new("ScreenGui")
    gui.Name = name
    gui.ResetOnSpawn = false
    gui.IgnoreGuiInset = true
    gui.DisplayOrder = 999
    if getgenv().protectgui then pcall(getgenv().protectgui, gui) end
    
    pcall(function() gui.Parent = game:GetService("CoreGui") end)
    if not gui.Parent then 
        local pgui = LocalPlayer:FindFirstChild("PlayerGui")
        if pgui then gui.Parent = pgui else
            task.spawn(function()
                gui.Parent = LocalPlayer:WaitForChild("PlayerGui")
            end)
        end
    end
    return gui
end

local target_line = (function()
    local LineGui = CreateOverlayGUI("TargetLineGui")

    local LineFrame = Instance.new("Frame")
    LineFrame.Parent = LineGui
    LineFrame.AnchorPoint = Vector2.new(0.5, 0.5)
    LineFrame.BorderSizePixel = 0
    LineFrame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    LineFrame.Active = false
    pcall(function() LineFrame.Interactable = false end)
    
    local line = {
        Visible = false,
        From = Vector2.new(0, 0),
        To = Vector2.new(0, 0),
        Color = Color3.fromRGB(255, 0, 0),
        Thickness = 2
    }
    
    function line:Update()
        LineFrame.Visible = self.Visible
        if self.Visible then
            local dist = (self.To - self.From).Magnitude
            LineFrame.Size = UDim2.new(0, dist, 0, self.Thickness)
            local center = (self.From + self.To) / 2
            LineFrame.Position = UDim2.new(0, center.X, 0, center.Y)
            LineFrame.Rotation = math.deg(math.atan2(self.To.Y - self.From.Y, self.To.X - self.From.X))
            LineFrame.BackgroundColor3 = self.Color
        end
    end
    
    return line
end)()

local fov_circle = (function()
    local FOVGui = CreateOverlayGUI("FOVCircleGui")

    local FOVContainer = Instance.new("Frame")
    FOVContainer.Parent = FOVGui
    FOVContainer.AnchorPoint = Vector2.new(0.5, 0.5)
    FOVContainer.Position = UDim2.new(0.5, 0, 0.5, 0)
    FOVContainer.BackgroundTransparency = 1
    FOVContainer.Size = UDim2.new(0, 0, 0, 0)
    FOVContainer.Active = false
    pcall(function() FOVContainer.Interactable = false end)
    
    local lines = {}
    for i = 1, 19 do
        local line = Instance.new("Frame")
        line.Parent = FOVContainer
        line.AnchorPoint = Vector2.new(0.5, 0.5)
        line.BorderSizePixel = 0
        line.BackgroundColor3 = Color3.fromRGB(240, 240, 240)
        
        local angle = math.rad((i - 1) * (360 / 19))
        table.insert(lines, {frame = line, angle = angle})
    end

    local circle = {
        Visible = false,
        Radius = 130,
        Position = Vector2.new(0, 0),
        Color = Color3.fromRGB(255, 255, 255),
        Transparency = 1,
        Rotation = 0,
        Lines = lines,
        Container = FOVContainer
    }
    
    function circle:Update()
        self.Container.Visible = self.Visible
        if self.Visible then
            self.Container.Position = UDim2.new(0, self.Position.X, 0, self.Position.Y)
            self.Container.Rotation = self.Rotation
            
            local d = self.Radius * math.cos(math.pi / 19)
            local s = 2 * self.Radius * math.sin(math.pi / 19)
            
            for i, lineData in ipairs(self.Lines) do
                local line = lineData.frame
                line.BackgroundColor3 = self.Color
                line.BackgroundTransparency = 1 - self.Transparency
                
                local angle = lineData.angle
                local x = math.cos(angle) * d
                local y = math.sin(angle) * d
                
                line.Position = UDim2.new(0, x, 0, y)
                line.Rotation = math.deg(angle) + 90
                line.Size = UDim2.new(0, s + 3, 0, 2)
            end
        end
    end
    
    return circle
end)()

local check360_display = (function()
    local text = Drawing.new("Text")
    text.Size = 25
    text.Center = true
    text.Outline = true
    text.Color = Color3.fromRGB(255, 255, 255)
    text.ZIndex = 2
    text.Visible = false
    text.Position = Vector2.new(Camera.ViewportSize.X / 2, 50)
    
    return text
end)()

local coord_marker = (function()
    local circle = Drawing.new("Circle")
    circle.Radius = 30
    circle.Thickness = 2
    circle.Filled = false
    circle.Color = Color3.fromRGB(255, 0, 0)
    circle.Visible = false
    circle.ZIndex = 10
    return circle
end)()

local coord_text = (function()
    local text = Drawing.new("Text")
    text.Size = 18
    text.Center = true
    text.Outline = true
    text.Color = Color3.fromRGB(255, 0, 0)
    text.Visible = false
    text.ZIndex = 11
    return text
end)()

local inv_fov_circle = Drawing.new("Circle")
inv_fov_circle.Thickness = 1
inv_fov_circle.NumSides = 64
inv_fov_circle.Color = Color3.fromRGB(255, 255, 255)
inv_fov_circle.Filled = false
inv_fov_circle.Visible = false

local InventoryEspObjects = {}
local function createInventoryESP(player)
    local esp = {
        ItemLabel = Instance.new("TextLabel"),
        ItemIcon = Instance.new("ImageLabel")
    }
    
    local gui = CreateOverlayGUI("InventoryEsp_" .. player.Name)
    
    esp.ItemLabel.BackgroundTransparency = 1
    esp.ItemLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
    esp.ItemLabel.TextStrokeTransparency = 0.5
    esp.ItemLabel.Font = Enum.Font.Code
    esp.ItemLabel.TextSize = 14
    esp.ItemLabel.Parent = gui
    
    esp.ItemIcon.BackgroundTransparency = 1
    esp.ItemIcon.ScaleType = Enum.ScaleType.Fit
    esp.ItemIcon.Parent = gui
    
    InventoryEspObjects[player] = esp
end

-- Backpack UI
local backpack_gui = CreateOverlayGUI("BackpackViewer")
local backpack_frame = Instance.new("Frame", backpack_gui)
backpack_frame.AnchorPoint = Vector2.new(1, 0.5)
backpack_frame.Size = UDim2.new(0, 240, 0, 300)
backpack_frame.Position = UDim2.new(1, -20, 0.5, 0)
backpack_frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
backpack_frame.BorderSizePixel = 0
backpack_frame.Visible = false

local backpack_title = Instance.new("TextLabel", backpack_frame)
backpack_title.Size = UDim2.new(1, 0, 0, 25)
backpack_title.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
backpack_title.TextColor3 = Color3.fromRGB(255, 255, 255)
backpack_title.Text = " Inventory"
backpack_title.Font = Enum.Font.Code
backpack_title.TextSize = 14
backpack_title.Active = true

local backpack_list = Instance.new("ScrollingFrame", backpack_frame)
backpack_list.Size = UDim2.new(1, 0, 1, -25)
backpack_list.Position = UDim2.new(0, 0, 0, 25)
backpack_list.BackgroundTransparency = 1
backpack_list.ScrollBarThickness = 4
backpack_list.CanvasSize = UDim2.new(0, 0, 0, 0)

local backpack_layout = Instance.new("UIGridLayout", backpack_list)
backpack_layout.CellSize = UDim2.new(0, 45, 0, 45)
backpack_layout.CellPadding = UDim2.new(0, 5, 0, 5)

-- Dragging logic
do
    local dragging, dragStart, startPos

    local function update(input)
        local delta = input.Position - dragStart
        backpack_frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end

    backpack_title.InputBegan:Connect(function(input)
        if SilentAimSettings.BackpackDraggable and (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then
            dragging = true
            dragStart = input.Position
            startPos = backpack_frame.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    backpack_title.InputChanged:Connect(function(input)
        if SilentAimSettings.BackpackDraggable and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            if dragging then
                update(input)
            end
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            update(input)
        end
    end)
end

local ExpectedArguments = {
    FindPartOnRayWithIgnoreList = {
        ArgCountRequired = 3,
        Args = {
            "Instance", "Ray", "table", "boolean", "boolean"
        }
    },
    FindPartOnRayWithWhitelist = {
        ArgCountRequired = 3,
        Args = {
            "Instance", "Ray", "table", "boolean"
        }
    },
    FindPartOnRay = {
        ArgCountRequired = 2,
        Args = {
            "Instance", "Ray", "Instance", "boolean", "boolean"
        }
    },
    Raycast = {
        ArgCountRequired = 3,
        Args = {
            "Instance", "Vector3", "Vector3", "RaycastParams"
        }
    }
}

function CalculateChance(Percentage)
    Percentage = math.floor(Percentage)
    local chance = math.floor(Random.new().NextNumber(Random.new(), 0, 1) * 100) / 100
    return chance <= Percentage / 100
end

local function getPositionOnScreen(Vector)
    local Vec3, OnScreen = WorldToScreen(Camera, Vector)
    return Vector2.new(Vec3.X, Vec3.Y), OnScreen
end

local function ValidateArguments(Args, RayMethod)
    local Matches = 0
    if #Args < RayMethod.ArgCountRequired then
        return false
    end
    for Pos, Argument in next, Args do
        if typeof(Argument) == RayMethod.Args[Pos] then
            Matches = Matches + 1
        end
    end
    return Matches >= RayMethod.ArgCountRequired
end

local function getDirection(Origin, Position)
    return (Position - Origin).Unit * 1000
end

local function getMousePosition()
    return Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
end

local function HasLineOfSight(targetPart)
    if not (targetPart and targetPart.Parent) then return false end
    if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then return false end
    local origin = LocalPlayer.Character.HumanoidRootPart.Position
    local destination = targetPart.Position
    local direction = destination - origin
    
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    raycastParams.FilterDescendantsInstances = {LocalPlayer.Character, targetPart.Parent}
    
    local result = workspace:Raycast(origin, direction, raycastParams)
    return result == nil
end

local function IsPlayerVisible(Player)
    if not (Player and Player.Character and LocalPlayer.Character) then return false end 
    
    local Target = FindFirstChild(Player.Character, SilentAimSettings.TargetPart) or FindFirstChild(Player.Character, "HumanoidRootPart")
    
    if not Target then return false end 
    
    local CastPoints = {Target.Position, LocalPlayer.Character, Player.Character}
    local IgnoreList = {LocalPlayer.Character, Player.Character}
    local ObscuringObjects = #GetPartsObscuringTarget(Camera, CastPoints, IgnoreList)
    
    return ObscuringObjects == 0
end

local function getClosestPlayer()
    if not SilentAimSettings.TargetPart then return end
    local Closest
    local DistanceToMouse
    for _, Player in ipairs(GetPlayers(Players)) do
        if Player == LocalPlayer then continue end
        if SilentAimSettings.TeamCheck and Player.Team == LocalPlayer.Team then continue end

        local Character = Player.Character
        if not Character then continue end
        
        if SilentAimSettings.VisibleCheck and not IsPlayerVisible(Player) then continue end

        local HumanoidRootPart = FindFirstChild(Character, "HumanoidRootPart")
        local Humanoid = FindFirstChild(Character, "Humanoid")
        if not HumanoidRootPart or not Humanoid or Humanoid.Health <= 0 then continue end

        local ScreenPosition, OnScreen = getPositionOnScreen(HumanoidRootPart.Position)
        if not OnScreen then continue end

        local Distance = (getMousePosition() - ScreenPosition).Magnitude
        if Distance <= (DistanceToMouse or SilentAimSettings.FOVRadius or 2000) then
            Closest = ((SilentAimSettings.TargetPart == "Random" and Character[ValidTargetParts[math.random(1, #ValidTargetParts)]]) or Character[SilentAimSettings.TargetPart])
            DistanceToMouse = Distance
        end
    end
    return Closest
end

local function GetBestInventoryTarget()
    local best, dist = nil, SilentAimSettings.InventoryFovRadius
    local cam = workspace.CurrentCamera
    local screenCenter = Vector2.new(cam.ViewportSize.X/2, cam.ViewportSize.Y/2)
    
    if SilentAimSettings.BackpackTargetMode == "Closest Player" then
        dist = math.huge
        local lpRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not lpRoot then return nil end
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                local root = p.Character:FindFirstChild("HumanoidRootPart")
                local hum = p.Character:FindFirstChild("Humanoid")
                if root and hum and hum.Health > 0 then
                    local d = (root.Position - lpRoot.Position).Magnitude
                    if d < dist then dist = d; best = p end
                end
            end
        end
    else
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                local root = p.Character:FindFirstChild("HumanoidRootPart")
                local hum = p.Character:FindFirstChild("Humanoid")
                if root and hum and hum.Health > 0 then
                    local pos, onS = cam:WorldToViewportPoint(root.Position)
                    if onS then
                        local d = (Vector2.new(pos.X, pos.Y) - screenCenter).Magnitude
                        if d < dist then dist = d; best = p end
                    end
                end
            end
        end
    end
    return best
end

local Tabs = {
	Main = Window:AddTab("Main", "crosshair"),
	Combat = Window:AddTab("Combat", "swords"),
	Visuals = Window:AddTab("Visuals", "eye"),
	Movement = Window:AddTab("Movement", "fast-forward"),
    Misc = Window:AddTab("Misc", "list"),
	["UI Settings"] = Window:AddTab("UI Settings", "settings"),
}

local MainGroupBox = Tabs.Main:AddLeftGroupbox("Silent Aim")
local CombatGroupBox = Tabs.Combat:AddLeftGroupbox("Combat Settings")
local VisualsGroupBox1 = Tabs.Visuals:AddLeftGroupbox("Player Visuals")
local VisualsGroupBox2 = Tabs.Visuals:AddRightGroupbox("World & FOV")
local MovementGroupBox = Tabs.Movement:AddLeftGroupbox("Movement")
local MiscGroupBox = Tabs.Misc:AddLeftGroupbox("Miscellaneous")
local InventoryGroupBox = Tabs.Misc:AddRightGroupbox("Inventory Viewer")

InventoryGroupBox:AddToggle("InventoryEspToggle", {
    Text = "Enable Item ESP (Icons)",
    Default = SilentAimSettings.InventoryEspEnabled,
    Callback = function(Value)
        SilentAimSettings.InventoryEspEnabled = Value
    end,
})

InventoryGroupBox:AddToggle("BackpackViewToggle", {
    Text = "Enable Backpack Window",
    Default = SilentAimSettings.BackpackViewEnabled,
    Callback = function(Value)
        SilentAimSettings.BackpackViewEnabled = Value
    end,
})

InventoryGroupBox:AddToggle("BackpackDraggableToggle", {
    Text = "Draggable Window",
    Default = SilentAimSettings.BackpackDraggable,
    Callback = function(Value)
        SilentAimSettings.BackpackDraggable = Value
    end,
})

InventoryGroupBox:AddDropdown("BackpackTargetModeDrop", {
    Text = "Target Method",
    Values = {"FOV Target", "Closest Player"},
    Default = 1,
    Multi = false,
    Callback = function(Value)
        SilentAimSettings.BackpackTargetMode = Value
    end,
})

InventoryGroupBox:AddSlider("InventoryFovSlider", {
    Text = "Selection FOV Radius",
    Default = SilentAimSettings.InventoryFovRadius,
    Min = 10,
    Max = 1000,
    Rounding = 0,
    Suffix = "px",
    Callback = function(Value)
        SilentAimSettings.InventoryFovRadius = Value
    end,
})

local StaffGroupBox = Tabs.Misc:AddRightGroupbox("Staff List")

StaffGroupBox:AddToggle("StaffListToggle", {
    Text = "Staff List Enabled",
    Default = StaffSettings.Enabled,
    Callback = function(Value)
        StaffSettings.Enabled = Value
        UpdateStaffVisibility()
    end,
})

StaffGroupBox:AddToggle("StaffOnlyOnlineToggle", {
    Text = "Only Online Staff",
    Default = StaffSettings.OnlyOnline,
    Callback = function(Value)
        StaffSettings.OnlyOnline = Value
    end,
})

CombatGroupBox:AddToggle("AntiAimToggle", {
    Text = "Enable Anti-Aim (Fast Spin)",
    Default = SilentAimSettings.AntiAimEnabled,
    Callback = function(Value)
        SilentAimSettings.AntiAimEnabled = Value
    end,
})

CombatGroupBox:AddToggle("HitboxExpanderToggle", {
    Text = "Hitbox Expander",
    Default = SilentAimSettings.HitboxExpander,
    Callback = function(Value)
        SilentAimSettings.HitboxExpander = Value
    end,
})

CombatGroupBox:AddDropdown("HitboxTypeDropdown", {
    Text = "Hitbox Part",
    Values = {"Head", "Body"},
    Default = 1,
    Multi = false,
    Callback = function(Value)
        SilentAimSettings.HitboxType = Value
    end,
})

CombatGroupBox:AddSlider("HitboxSizeSlider", {
    Text = "Hitbox Size",
    Default = SilentAimSettings.HitboxSize,
    Min = 0,
    Max = 10,
    Rounding = 0,
    Suffix = " Studs",
    Callback = function(Value)
        SilentAimSettings.HitboxSize = Value
    end,
})

CombatGroupBox:AddToggle("NoRecoilToggle", {
    Text = "No Recoil",
    Default = SilentAimSettings.NoRecoil,
    Callback = function(Value)
        SilentAimSettings.NoRecoil = Value
    end,
})

CombatGroupBox:AddToggle("NoSpreadToggle", {
    Text = "No Spread",
    Default = SilentAimSettings.NoSpread,
    Callback = function(Value)
        SilentAimSettings.NoSpread = Value
    end,
})

MainGroupBox:AddToggle("AimbotToggle", {
    Text = "Enable Silent Aim",
    Default = SilentAimSettings.Enabled,
    Callback = function(Value)
        SilentAimSettings.Enabled = Value
        target_line.Visible = SilentAimSettings.Enabled and SilentAimSettings.ShowSilentAimTarget
    end,
})

MainGroupBox:AddToggle("TeamCheckToggle", {
    Text = "Team Check",
    Default = SilentAimSettings.TeamCheck,
    Callback = function(Value)
        SilentAimSettings.TeamCheck = Value
    end,
})

MainGroupBox:AddToggle("VisCheckToggle", {
    Text = "Visible Check",
    Default = SilentAimSettings.VisibleCheck,
    Callback = function(Value)
        SilentAimSettings.VisibleCheck = Value
    end,
})

MainGroupBox:AddDropdown("TargetPartDrop", {
    Text = "Target Part",
    Values = {"Head", "HumanoidRootPart", "Random"},
    Default = 2,
    Multi = false,
    Callback = function(Value)
        SilentAimSettings.TargetPart = Value
    end,
})

MainGroupBox:AddSlider("HitChanceSlider", {
    Text = "Hit Chance",
    Default = SilentAimSettings.HitChance,
    Min = 0,
    Max = 100,
    Rounding = 0,
    Suffix = "%",
    Callback = function(Value)
        SilentAimSettings.HitChance = Value
    end,
})

VisualsGroupBox1:AddToggle("ThirdPersonToggle", {
    Text = "Third Person Mode",
    Default = SilentAimSettings.ThirdPersonEnabled,
    Callback = function(Value)
        SilentAimSettings.ThirdPersonEnabled = Value
        if not Value then
            pcall(function()
                LocalPlayer.CameraMode = Enum.CameraMode.Classic
                LocalPlayer.CameraMaxZoomDistance = 12.5
            end)
        end
    end,
})

VisualsGroupBox1:AddSlider("ThirdPersonDistSlider", {
    Text = "Third Person Distance",
    Default = SilentAimSettings.ThirdPersonDistance,
    Min = 0,
    Max = 100,
    Rounding = 0,
    Suffix = " Studs",
    Callback = function(Value)
        SilentAimSettings.ThirdPersonDistance = Value
    end,
})

VisualsGroupBox1:AddToggle("Check360Toggle", {
    Text = "360 Check (Nearby Players)",
    Default = SilentAimSettings.Check360Enabled,
    Callback = function(Value)
        SilentAimSettings.Check360Enabled = Value
    end,
})

VisualsGroupBox1:AddToggle("FOVVisibleToggle", {
    Text = "Show FOV Circle",
    Default = SilentAimSettings.FOVVisible,
    Callback = function(Value)
        SilentAimSettings.FOVVisible = Value
        fov_circle.Visible = Value
    end,
})

VisualsGroupBox1:AddToggle("SkyboxToggle", {
    Text = "Enable Skybox (Custom)",
    Default = SilentAimSettings.SkyboxEnabled,
    Callback = function(Value)
        SilentAimSettings.SkyboxEnabled = Value
        local lighting = game:GetService("Lighting")
        local sky = lighting:FindFirstChild("CustomSky")
        if Value then
            if not sky then
                sky = Instance.new("Sky")
                sky.Name = "CustomSky"
                sky.Parent = lighting
            end
            local id = "rbxassetid://97446575154133"
            sky.SkyboxBk = id
            sky.SkyboxDn = id
            sky.SkyboxFt = id
            sky.SkyboxLf = id
            sky.SkyboxRt = id
            sky.SkyboxUp = id
        else
            if sky then
                sky:Destroy()
            end
        end
    end,
})

VisualsGroupBox1:AddSlider("FOVRadSlider", {
    Text = "FOV Radius",
    Default = SilentAimSettings.FOVRadius,
    Min = 0,
    Max = 800,
    Rounding = 0,
    Suffix = "px",
    Callback = function(Value)
        SilentAimSettings.FOVRadius = Value
        fov_circle.Radius = Value
    end,
})

VisualsGroupBox2:AddToggle("TargetBoxToggle", {
    Text = "Show Target Tracer",
    Default = SilentAimSettings.ShowSilentAimTarget,
    Callback = function(Value)
        SilentAimSettings.ShowSilentAimTarget = Value
        target_line.Visible = Value
    end,
})


VisualsGroupBox2:AddToggle("SimpleESPEnabledToggle", {
    Text = "Simple ESP (No Lag)",
    Default = SilentAimSettings.SimpleESPEnabled,
    Callback = function(Value)
        SilentAimSettings.SimpleESPEnabled = Value
    end,
})
VisualsGroupBox2:AddToggle("ESPEnabledToggle", {
    Text = "Enable ESP",
    Default = SilentAimSettings.ESPEnabled,
    Callback = function(Value)
        SilentAimSettings.ESPEnabled = Value
    end,
})

VisualsGroupBox2:AddToggle("MorningTimeToggle", {
    Text = "Always Morning",
    Default = SilentAimSettings.MorningTime,
    Callback = function(Value)
        SilentAimSettings.MorningTime = Value
        if Value then
            game:GetService("Lighting").TimeOfDay = "08:00:00"
        end
    end,
})

VisualsGroupBox2:AddToggle("NoGrassToggle", {
    Text = "No Grass",
    Default = SilentAimSettings.NoGrass,
    Callback = function(Value)
        SilentAimSettings.NoGrass = Value
        if Value then
            local terrain = workspace:FindFirstChildOfClass("Terrain")
            if terrain then
                pcall(function() sethiddenproperty(terrain, "Decoration", false) end)
            end
        end
    end,
})

VisualsGroupBox2:AddToggle("PotatoGraphicsToggle", {
    Text = "Potato Graphics",
    Default = SilentAimSettings.PotatoGraphics,
    Callback = function(Value)
        SilentAimSettings.PotatoGraphics = Value
        if Value then
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("BasePart") and not v:IsA("MeshPart") then
                    v.Material = Enum.Material.SmoothPlastic
                elseif v:IsA("Texture") or v:IsA("Decal") then
                    v:Destroy()
                end
            end
        end
    end,
})

VisualsGroupBox2:AddToggle("BackpackESPEnabledToggle", {
    Text = "Enable Backpack ESP",
    Default = SilentAimSettings.DeathESPEnabled,
    Callback = function(Value)
        SilentAimSettings.DeathESPEnabled = Value
    end,
})

VisualsGroupBox2:AddToggle("BulletTracerToggle", {
    Text = "Show Bullet Tracers",
    Default = SilentAimSettings.ShowBulletTracers,
    Callback = function(Value)
        SilentAimSettings.ShowBulletTracers = Value
    end,
})

VisualsGroupBox2:AddToggle("HitNotifyToggle", {
    Text = "Show Hit Notifications",
    Default = SilentAimSettings.ShowHitNotifications,
    Callback = function(Value)
        SilentAimSettings.ShowHitNotifications = Value
    end,
})

VisualsGroupBox2:AddToggle("CustomFOVToggle", {
    Text = "Custom FOV (Stretch Screen)",
    Default = SilentAimSettings.CustomCameraFOV,
    Callback = function(Value)
        SilentAimSettings.CustomCameraFOV = Value
    end,
})

VisualsGroupBox2:AddSlider("CameraFOVSlider", {
    Text = "Camera FOV Amount",
    Default = SilentAimSettings.CameraFOVAmount,
    Min = 70,
    Max = 120,
    Rounding = 0,
    Suffix = "°",
    Callback = function(Value)
        SilentAimSettings.CameraFOVAmount = Value
    end,
})

MovementGroupBox:AddToggle("VflyToggle", {
    Text = "Enable Vfly (Vehicle Fly)",
    Default = SilentAimSettings.VflyEnabled,
    Callback = function(Value)
        SilentAimSettings.VflyEnabled = Value
    end,
})

MovementGroupBox:AddSlider("VflySpeedSlider", {
    Text = "Vfly Speed",
    Default = SilentAimSettings.VflySpeed,
    Min = 1,
    Max = 10,
    Rounding = 0,
    Suffix = "x",
    Callback = function(Value)
        SilentAimSettings.VflySpeed = Value
    end,
})

MovementGroupBox:AddToggle("SpiderToggle", {
    Text = "Spider (Climb Walls)",
    Default = SilentAimSettings.SpiderEnabled,
    Callback = function(Value)
        SilentAimSettings.SpiderEnabled = Value
    end,
})

MovementGroupBox:AddSlider("SpiderSpeedSlider", {
    Text = "Spider Speed",
    Default = SilentAimSettings.SpiderSpeed,
    Min = 10,
    Max = 100,
    Rounding = 0,
    Callback = function(Value)
        SilentAimSettings.SpiderSpeed = Value
    end,
})

MovementGroupBox:AddToggle("DesyncToggle", {
    Text = "Desync (Show Button)",
    Default = SilentAimSettings.DesyncEnabled,
    Callback = function(Value)
        DesyncGui.Enabled = Value
        if not Value then
            SilentAimSettings.DesyncEnabled = false
            DesyncButton.Text = "Desync: OFF"
        end
    end,
})

MiscGroupBox:AddToggle("FlyJumpToggle", {
    Text = "Enable Fly Jump",
    Default = SilentAimSettings.FlyJumpEnabled,
    Callback = function(Value)
        SilentAimSettings.FlyJumpEnabled = Value
    end,
})

MiscGroupBox:AddToggle("ZoomButtonToggle", {
    Text = "Enable Zoom Button",
    Default = SilentAimSettings.ZoomButtonEnabled,
    Callback = function(Value)
        SilentAimSettings.ZoomButtonEnabled = Value
        if getgenv().ZoomToggleGui then
            getgenv().ZoomToggleGui.Enabled = Value
        end
    end,
})

MiscGroupBox:AddToggle("UwUSoundToggle", {
    Text = "UwU Hit Sound (Replaces HitServer)",
    Default = SilentAimSettings.UwUHitSound,
    Callback = function(Value)
        SilentAimSettings.UwUHitSound = Value
        if Value then
            local function replaceNow(parent)
                for _, v in pairs(parent:GetDescendants()) do
                    if v:IsA("Sound") and v.Name == "HitServer" and v.SoundId ~= "rbxassetid://139219844119994" then
                        v.SoundId = "rbxassetid://139219844119994"
                    end
                end
            end
            pcall(function() replaceNow(workspace) end)
            pcall(function() replaceNow(game:GetService("Players")) end)
            pcall(function() replaceNow(game:GetService("ReplicatedStorage")) end)
        end
    end,
})

MiscGroupBox:AddToggle("CuteSoundToggle", {
    Text = "Cute Open Sound (Nya)",
    Default = SilentAimSettings.CuteOpenSound,
    Callback = function(Value)
        SilentAimSettings.CuteOpenSound = Value
        if Value then
            local function replaceNow(parent)
                for _, v in pairs(parent:GetDescendants()) do
                    if v:IsA("Sound") and v.Name == "PlayerOpenSound" and v.SoundId ~= "rbxassetid://137965684634919" then
                        v.SoundId = "rbxassetid://137965684634919"
                    end
                end
            end
            pcall(function() replaceNow(workspace) end)
            pcall(function() replaceNow(game:GetService("Players")) end)
            pcall(function() replaceNow(game:GetService("ReplicatedStorage")) end)
        end
    end,
})

MiscGroupBox:AddToggle("AutoHempToggle", {
    Text = "Auto Hemp (Auto Collect)",
    Default = SilentAimSettings.AutoHempEnabled,
    Callback = function(Value)
        SilentAimSettings.AutoHempEnabled = Value
    end,
})

MiscGroupBox:AddToggle("KillNotifyToggle", {
    Text = "Kill Notification",
    Default = SilentAimSettings.KillNotifyEnabled,
    Callback = function(Value)
        SilentAimSettings.KillNotifyEnabled = Value
    end,
})

MiscGroupBox:AddToggle("DeleteSpikesToggle", {
    Text = "Delete Spikes",
    Default = SilentAimSettings.DeleteSpikesEnabled,
    Callback = function(Value)
        toggleDeleteSpikes(Value)
    end,
})

local function checkSound(v)
    if v:IsA("Sound") then
        if v.Name == "HitServer" then
            if SilentAimSettings.UwUHitSound then
                v.SoundId = "rbxassetid://139219844119994"
            end
            v:GetPropertyChangedSignal("SoundId"):Connect(function()
                if SilentAimSettings.UwUHitSound and v.SoundId ~= "rbxassetid://139219844119994" then
                    v.SoundId = "rbxassetid://139219844119994"
                end
            end)
        elseif v.Name == "PlayerOpenSound" then
            if SilentAimSettings.CuteOpenSound then
                v.SoundId = "rbxassetid://137965684634919"
            end
            v:GetPropertyChangedSignal("SoundId"):Connect(function()
                if SilentAimSettings.CuteOpenSound and v.SoundId ~= "rbxassetid://137965684634919" then
                    v.SoundId = "rbxassetid://137965684634919"
                end
            end)
        end
    end
end

pcall(function()
    for _, v in pairs(workspace:GetDescendants()) do checkSound(v) end
    for _, v in pairs(game:GetService("Players"):GetDescendants()) do checkSound(v) end
    for _, v in pairs(game:GetService("ReplicatedStorage"):GetDescendants()) do checkSound(v) end

    workspace.DescendantAdded:Connect(checkSound)
    game:GetService("Players").DescendantAdded:Connect(checkSound)
    game:GetService("ReplicatedStorage").DescendantAdded:Connect(checkSound)
end)

MiscGroupBox:AddToggle("WeaponSkinToggle", {
    Text = "Enable Custom Weapon Skin",
    Default = SilentAimSettings.WeaponSkinEnabled,
    Callback = function(Value)
        SilentAimSettings.WeaponSkinEnabled = Value
        if Value and LocalPlayer.Character and _G.hookCharacter then
            pcall(function() _G.hookCharacter(LocalPlayer.Character) end)
        end
    end,
})

local SKIN_ID = "rbxassetid://109571469973979"
local appliedWeaponSkins = {}

local function applyWeaponSkin(part)
    if not part or not part:IsA("BasePart") then return end
    if part.Transparency >= 0.9 then return end

    local n = part.Name:lower()
    if n:find("arm") or n:find("hand") or n:find("leg") or n:find("torso") or n:find("head") then return end

    if appliedWeaponSkins[part] then return end
    appliedWeaponSkins[part] = true

    pcall(function()
        if part:IsA("MeshPart") then
            part.TextureID = SKIN_ID
        end
    end)

    for _, v in pairs(part:GetChildren()) do
        if v:IsA("Texture") or v:IsA("Decal") or v:IsA("SurfaceAppearance") then
            pcall(function()
                v:Destroy()
            end)
        end
    end

    for _, f in pairs(Enum.NormalId:GetEnumItems()) do
        local t = Instance.new("Texture")
        t.Name = "CustomSkin"
        t.Texture = SKIN_ID
        t.Face = f
        t.Parent = part
    end

    part.CastShadow = false
end

local function applyToolSkin(tool)
    if not SilentAimSettings.WeaponSkinEnabled then return end
    for _, d in pairs(tool:GetDescendants()) do
        applyWeaponSkin(d)
    end
end

_G.hookCharacter = function(char)
    appliedWeaponSkins = {}

    char.ChildAdded:Connect(function(obj)
        if obj:IsA("Tool") then
            task.wait(0.1)
            applyToolSkin(obj)
        end
    end)

    for _, obj in pairs(char:GetChildren()) do
        if obj:IsA("Tool") then
            applyToolSkin(obj)
        end
    end
end

if LocalPlayer.Character then
    _G.hookCharacter(LocalPlayer.Character)
end
LocalPlayer.CharacterAdded:Connect(function(char)
    _G.hookCharacter(char)
end)

local flyJumpStart = 0
local flyActive = false
local lastJumpRequest = 0

UserInputService.JumpRequest:Connect(function()
    lastJumpRequest = tick()
    if SilentAimSettings.FlyJumpEnabled then
        local character = LocalPlayer.Character
        local humanoid = character and character:FindFirstChildOfClass("Humanoid")
        local hrp = character and character:FindFirstChild("HumanoidRootPart")

        if humanoid and hrp and humanoid.Health > 0 then
            humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
            
            if humanoid.FloorMaterial ~= Enum.Material.Air then
                flyJumpStart = tick()
                flyActive = true
            elseif not flyActive then
                flyJumpStart = tick()
                flyActive = true
            end
        end
    end
end)

local defaultFOV = 70
local zooming = false
local function CreateZoomButton()
    local gui = CreateOverlayGUI("ZoomButtonGui")
    getgenv().ZoomToggleGui = gui
    gui.Enabled = SilentAimSettings.ZoomButtonEnabled
    
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 60, 0, 60)
    btn.Position = UDim2.new(1, -80, 0.5, -30)
    btn.BackgroundColor3 = Color3.fromRGB(150, 150, 150)
    btn.Text = ""
    btn.Parent = gui
    btn.Active = true
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = btn
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(0, 0, 0)
    stroke.Thickness = 3
    stroke.Parent = btn
    
    local icon = Instance.new("ImageLabel")
    icon.Size = UDim2.new(0.6, 0, 0.6, 0)
    icon.Position = UDim2.new(0.2, 0, 0.2, 0)
    icon.BackgroundTransparency = 1
    icon.Image = "rbxassetid://74200505949194"
    icon.ImageColor3 = Color3.fromRGB(255, 255, 255)
    icon.Parent = btn
    
    btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            zooming = true
        end
    end)
    btn.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            zooming = false
            if not SilentAimSettings.CustomCameraFOV then
                Camera.FieldOfView = defaultFOV
            end
        end
    end)
end

CreateZoomButton()

local FlyJumpGui = CreateOverlayGUI("FlyJumpGui")

local FlyJumpContainer = Instance.new("Frame")
FlyJumpContainer.Size = UDim2.new(0, 300, 0, 14)
FlyJumpContainer.Position = UDim2.new(0.5, 0, 0.75, 0)
FlyJumpContainer.AnchorPoint = Vector2.new(0.5, 0.5)
FlyJumpContainer.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
FlyJumpContainer.BorderSizePixel = 0
FlyJumpContainer.Visible = false
FlyJumpContainer.Parent = FlyJumpGui

local FlyJumpBar = Instance.new("Frame")
FlyJumpBar.Size = UDim2.new(0, 0, 1, 0)
FlyJumpBar.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
FlyJumpBar.BorderSizePixel = 0
FlyJumpBar.Parent = FlyJumpContainer

local FlyJumpText = Instance.new("TextLabel")
FlyJumpText.Size = UDim2.new(1, 0, 0, 30)
FlyJumpText.Position = UDim2.new(0, 0, 1, 10)
FlyJumpText.BackgroundTransparency = 1
FlyJumpText.TextColor3 = Color3.fromRGB(255, 255, 255)
FlyJumpText.TextSize = 25
FlyJumpText.Font = Enum.Font.GothamBold
FlyJumpText.Text = "0.0S"
FlyJumpText.Parent = FlyJumpContainer

local function getESPBox(character, camera)
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return false end
    
    local rootPos, onScreen = camera:WorldToViewportPoint(hrp.Position)
    if not onScreen then return false end
    
    local cf, size = character:GetBoundingBox()
    
    local topPos = camera:WorldToViewportPoint(cf.Position + Vector3.new(0, size.Y/2, 0))
    local bottomPos = camera:WorldToViewportPoint(cf.Position - Vector3.new(0, size.Y/2, 0))
    
    local height = math.abs(topPos.Y - bottomPos.Y)
    local width = height * 0.6
    
    local topY = math.min(topPos.Y, bottomPos.Y)
    
    return true, Vector2.new(rootPos.X - width/2, topY), Vector2.new(width, height), rootPos.Z
end


local SimpleESPObjects = {}

local function createSimpleESP()
    local Box = Drawing.new("Square")
    Box.Thickness = 1
    Box.Filled = false
    Box.Color = Color3.fromRGB(0, 255, 0)
    Box.ZIndex = 1
    
    local Name = Drawing.new("Text")
    Name.Size = 14
    Name.Center = true
    Name.Color = Color3.fromRGB(255, 255, 255)
    Name.Outline = true
    Name.ZIndex = 2
    
    local HealthText = Drawing.new("Text")
    HealthText.Size = 12
    HealthText.Center = true
    HealthText.Color = Color3.fromRGB(0, 255, 0)
    HealthText.Outline = true
    HealthText.ZIndex = 2
    
    return {
        Box = Box,
        Name = Name,
        HealthText = HealthText,
        SetVisible = function(self, vis)
            self.Box.Visible = vis
            self.Name.Visible = vis
            self.HealthText.Visible = vis
        end,
        Destroy = function(self)
            self.Box:Remove()
            self.Name:Remove()
            self.HealthText:Remove()
        end
    }
end

local function getSimpleESPBox(character, camera)
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return false end
    
    local rootPos, onScreen = camera:WorldToViewportPoint(hrp.Position)
    if not onScreen then return false end
    
    local topPos = camera:WorldToViewportPoint(hrp.Position + Vector3.new(0, 3, 0))
    local bottomPos = camera:WorldToViewportPoint(hrp.Position - Vector3.new(0, 3.5, 0))
    
    local height = math.abs(topPos.Y - bottomPos.Y)
    local width = height * 0.6
    local topY = math.min(topPos.Y, bottomPos.Y)
    
    return true, Vector2.new(rootPos.X - width/2, topY), Vector2.new(width, height), rootPos.Z
end

local ESPObjects = {}

local function createESP()
    local Box = Drawing.new("Square")
    Box.Thickness = 1
    Box.Filled = false
    Box.Color = Color3.fromRGB(220, 220, 220)
    Box.ZIndex = 1
    
    local Name = Drawing.new("Text")
    Name.Size = 16
    Name.Center = true
    Name.Color = Color3.fromRGB(220, 220, 220)
    Name.Outline = true
    Name.ZIndex = 2
    
    local HealthBarBg = Drawing.new("Square")
    HealthBarBg.Thickness = 1
    HealthBarBg.Filled = true
    HealthBarBg.Color = Color3.fromRGB(0, 0, 0)
    HealthBarBg.ZIndex = 1
    
    local HealthBarFill = Drawing.new("Square")
    HealthBarFill.Thickness = 1
    HealthBarFill.Filled = true
    HealthBarFill.ZIndex = 2
    
    local HealthText = Drawing.new("Text")
    HealthText.Size = 14
    HealthText.Center = true
    HealthText.Color = Color3.fromRGB(220, 220, 220)
    HealthText.Outline = true
    HealthText.ZIndex = 2
    
    local EquippedText = Drawing.new("Text")
    EquippedText.Size = 14
    EquippedText.Center = true
    EquippedText.Color = Color3.fromRGB(255, 50, 50)
    EquippedText.Outline = true
    EquippedText.ZIndex = 2
    
    return {
        Box = Box,
        Name = Name,
        HealthBarBg = HealthBarBg,
        HealthBarFill = HealthBarFill,
        HealthText = HealthText,
        EquippedText = EquippedText,
        SetVisible = function(self, vis)
            self.Box.Visible = vis
            self.Name.Visible = vis
            self.HealthBarBg.Visible = vis
            self.HealthBarFill.Visible = vis
            self.HealthText.Visible = vis
            self.EquippedText.Visible = vis
        end,
        Destroy = function(self)
            self.Box:Remove()
            self.Name:Remove()
            self.HealthBarBg:Remove()
            self.HealthBarFill:Remove()
            self.HealthText:Remove()
            self.EquippedText:Remove()
        end
    }
end

local function createDeathESP()
    local Box = Drawing.new("Square")
    Box.Thickness = 1
    Box.Filled = false
    Box.Color = Color3.fromRGB(255, 255, 255)
    Box.ZIndex = 1
    
    local Name = Drawing.new("Text")
    Name.Size = 13
    Name.Center = true
    Name.Color = Color3.fromRGB(255, 255, 255)
    Name.Outline = true
    Name.ZIndex = 2
    Name.Text = "Backpack"
    
    local DistText = Drawing.new("Text")
    DistText.Size = 12
    DistText.Center = true
    DistText.Color = Color3.fromRGB(200, 200, 200)
    DistText.Outline = true
    DistText.ZIndex = 2
    
    return {
        Box = Box,
        Name = Name,
        DistText = DistText,
        SetVisible = function(self, vis)
            self.Box.Visible = vis
            self.Name.Visible = vis
            self.DistText.Visible = vis
        end,
        Destroy = function(self)
            self.Box:Remove()
            self.Name:Remove()
            self.DistText:Remove()
        end
    }
end

local DeathESPObjects = {}

for _, p in pairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then
        ESPObjects[p] = createESP()
        SimpleESPObjects[p] = createSimpleESP()
        createInventoryESP(p)
        hookDeath(p)
    end
end
Players.PlayerAdded:Connect(function(p)
    ESPObjects[p] = createESP()
    SimpleESPObjects[p] = createSimpleESP()
    createInventoryESP(p)
    hookDeath(p)
end)
Players.PlayerRemoving:Connect(function(p)
    if ESPObjects[p] then
        ESPObjects[p]:Destroy()
        ESPObjects[p] = nil
    end
    if SimpleESPObjects[p] then
        SimpleESPObjects[p]:Destroy()
        SimpleESPObjects[p] = nil
    end
    if InventoryEspObjects[p] then
        if InventoryEspObjects[p].ItemLabel then InventoryEspObjects[p].ItemLabel:Destroy() end
        if InventoryEspObjects[p].ItemIcon then InventoryEspObjects[p].ItemIcon:Destroy() end
        InventoryEspObjects[p] = nil
    end
end)

local fovRotation = 0
getgenv().FOVSpeedBoost = 0

local ReplicatedStorage = game:GetService("ReplicatedStorage")
task.spawn(function()
    while task.wait(1) do
        pcall(function()
            -- No Recoil
            local Recoil = require(ReplicatedStorage:WaitForChild("Gun"):WaitForChild("Scripts"):WaitForChild("RecoilHandler"))
            if Recoil and SilentAimSettings.NoRecoil then
                Recoil.nextStep = function() end
                Recoil.setRecoilMultiplier = function() end
            end
            
            -- No Spread
            local Utils = require(ReplicatedStorage:WaitForChild("Utils"))
            if Utils and SilentAimSettings.NoSpread then
                Utils.applySpreadToDirection = function(dir) return dir end
            end
            
            local GunClient = require(ReplicatedStorage.Gun.Scripts.GunClient)
            if GunClient and SilentAimSettings.NoSpread then
                GunClient.updateSpreadMult = function() return 0 end
            end
        end)
    end
end)

resume(create(function()
    RenderStepped:Connect(function(dt)
        local dtNum = typeof(dt) == "number" and dt or 0.016
        
        -- Always Morning
        if SilentAimSettings.MorningTime then
            game:GetService("Lighting").ClockTime = 8
        end

        -- Hitbox Expander
        if SilentAimSettings.HitboxExpander then
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local char = player.Character
                    local parts = {}
                    if SilentAimSettings.HitboxType == "Head" then
                        local head = char:FindFirstChild("Head")
                        if head then table.insert(parts, head) end
                    else
                        for _, v in pairs(char:GetChildren()) do
                            if v:IsA("BasePart") and (v.Name:find("Torso") or v.Name:find("Arm") or v.Name:find("Leg")) then
                                table.insert(parts, v)
                            end
                        end
                    end
                    
                    for _, part in pairs(parts) do
                        part.Size = Vector3.new(SilentAimSettings.HitboxSize, SilentAimSettings.HitboxSize, SilentAimSettings.HitboxSize)
                        part.Transparency = 0.5
                        part.CanCollide = false
                    end
                end
            end
        else
            -- Check for players needing reset (simplified reset logic)
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local char = player.Character
                    local head = char:FindFirstChild("Head")
                    if head and head.Transparency == 0.5 then
                        head.Size = Vector3.new(2, 1, 1)
                        head.Transparency = 0
                        head.CanCollide = true
                    end
                    for _, v in pairs(char:GetChildren()) do
                        if v:IsA("BasePart") and v.Transparency == 0.5 and (v.Name:find("Torso") or v.Name:find("Arm") or v.Name:find("Leg")) then
                            if v.Name:find("Torso") then
                                v.Size = Vector3.new(2, 2, 1)
                            else
                                v.Size = Vector3.new(1, 2, 1)
                            end
                            v.Transparency = 0
                            v.CanCollide = true
                        end
                    end
                end
            end
        end

        -- 360 Check
        if SilentAimSettings.Check360Enabled then
            local count = 0
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    -- Filter by team if requested
                    if SilentAimSettings.TeamCheck and player.Team == LocalPlayer.Team then continue end
                    
                    local hrp = player.Character.HumanoidRootPart
                    local dist = (hrp.Position - Camera.CFrame.Position).Magnitude
                    if dist <= 350 then -- Using 350 as distance as user mentioned 350 degrees (maybe they meant studs?)
                        count = count + 1
                    end
                end
            end
            check360_display.Visible = true
            check360_display.Text = "Players Around: " .. tostring(count)
            check360_display.Position = Vector2.new(Camera.ViewportSize.X / 2, 70)
        else
            check360_display.Visible = false
        end

        -- Third Person
        if SilentAimSettings.ThirdPersonEnabled then
            LocalPlayer.CameraMode = Enum.CameraMode.Classic
            LocalPlayer.CameraMaxZoomDistance = SilentAimSettings.ThirdPersonDistance
            LocalPlayer.CameraMinZoomDistance = SilentAimSettings.ThirdPersonDistance
        else
            LocalPlayer.CameraMinZoomDistance = 0.5
        end

        -- Anti Aim (Simple 180 Backwards - Upright Fix)
        if SilentAimSettings.AntiAimEnabled then
            local char = LocalPlayer.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if hrp then
                -- Get camera look direction
                local look = Camera.CFrame.LookVector
                -- Zero out the Y axis to ensure the character stays strictly upright
                local horizontalLook = Vector3.new(look.X, 0, look.Z).Unit
                
                -- Target direction is exactly opposite (180 degrees)
                local targetLook = -horizontalLook
                
                -- Set CFrame at current position facing target direction (strictly horizontal)
                hrp.CFrame = CFrame.lookAt(hrp.Position, hrp.Position + targetLook)
            end
        end

        -- Vfly Logic
        if SilentAimSettings.VflyEnabled then
            local char = LocalPlayer.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum and hum.SeatPart then
                local seat = hum.SeatPart
                local moveDir = Camera.CFrame.LookVector
                
                -- Basic fly: Move seat in camera direction
                seat.AssemblyLinearVelocity = moveDir * (SilentAimSettings.VflySpeed * 50)
                seat.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
            end
        end

        -- Spider Logic
        if SilentAimSettings.SpiderEnabled then
            local char = LocalPlayer.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if hrp then
                local raycastParams = RaycastParams.new()
                raycastParams.FilterType = Enum.RaycastFilterType.Exclude
                raycastParams.FilterDescendantsInstances = {char}
                
                -- Raycast in front of player
                local lookDir = hrp.CFrame.LookVector
                local result = workspace:Raycast(hrp.Position, lookDir * 1.5, raycastParams)
                
                if result then
                    -- If we hitting something, move up
                    hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, SilentAimSettings.SpiderSpeed, hrp.AssemblyLinearVelocity.Z)
                end
            end
        end

        if SilentAimSettings.ShowSilentAimTarget and SilentAimSettings.Enabled then
            local HitPart = getClosestPlayer()
            if HitPart then 
                local RootToViewportPoint, IsOnScreen = WorldToViewportPoint(Camera, HitPart.Position);
                
                if IsOnScreen then
                    target_line.Visible = true
                    target_line.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
                    target_line.To = Vector2.new(RootToViewportPoint.X, RootToViewportPoint.Y)
                else
                    target_line.Visible = false
                end
            else 
                target_line.Visible = false 
            end
        else
            target_line.Visible = false 
        end
        
        getgenv().FOVSpeedBoost = getgenv().FOVSpeedBoost > 0.1 and getgenv().FOVSpeedBoost * 0.95 or 0
        fovRotation = (fovRotation + (10 + getgenv().FOVSpeedBoost) * dtNum) % 360
        
        if SilentAimSettings.FOVVisible then 
            fov_circle.Visible = true
            fov_circle.Position = getMousePosition()
            fov_circle.Radius = SilentAimSettings.FOVRadius
            fov_circle.Color = Color3.fromHSV((tick() * 0.1) % 1, 1, 1)
            fov_circle.Rotation = fovRotation
        else
            fov_circle.Visible = false
        end
        if fov_circle.Update then fov_circle:Update() end
        if target_line.Update then target_line:Update() end
        
        if zooming then
            Camera.FieldOfView = 7.5
        elseif SilentAimSettings.CustomCameraFOV then
            Camera.FieldOfView = SilentAimSettings.CameraFOVAmount
        end
        
    -- Inventory FOV Circle
        inv_fov_circle.Visible = SilentAimSettings.BackpackTargetMode == "FOV Target" and SilentAimSettings.BackpackViewEnabled
        inv_fov_circle.Position = Vector2.new(workspace.CurrentCamera.ViewportSize.X/2, workspace.CurrentCamera.ViewportSize.Y/2)
        inv_fov_circle.Radius = SilentAimSettings.InventoryFovRadius
        
        -- Inventory ESP (Player Item ESP)
        for p, esp in pairs(InventoryEspObjects) do
            local char = p.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local root = char and char:FindFirstChild("HumanoidRootPart")
            local alive = hum and hum.Health > 0
            
            if SilentAimSettings.InventoryEspEnabled and alive and root then
                local pos, onS = workspace.CurrentCamera:WorldToViewportPoint(root.Position)
                if onS then
                    local tool = char:FindFirstChildOfClass("Tool")
                    if tool then
                        if tool.TextureId ~= "" then
                            esp.ItemIcon.Image = tool.TextureId
                            esp.ItemIcon.Size = UDim2.new(0, 30, 0, 30)
                            esp.ItemIcon.Position = UDim2.new(0, pos.X - 15, 0, pos.Y + 20)
                            esp.ItemIcon.Visible = true
                            esp.ItemLabel.Visible = false
                        else
                            esp.ItemLabel.Text = tool.Name
                            esp.ItemLabel.Position = UDim2.new(0, pos.X - 50, 0, pos.Y + 20)
                            esp.ItemLabel.Size = UDim2.new(0, 100, 0, 20)
                            esp.ItemLabel.Visible = true
                            esp.ItemIcon.Visible = false
                        end
                    else
                        esp.ItemLabel.Visible = false
                        esp.ItemIcon.Visible = false
                    end
                else
                    esp.ItemLabel.Visible = false
                    esp.ItemIcon.Visible = false
                end
            else
                esp.ItemLabel.Visible = false
                esp.ItemIcon.Visible = false
            end
        end
        
        if SilentAimSettings.SimpleESPEnabled then
            for player, esp in pairs(SimpleESPObjects) do
                local character = player.Character
                local humanoid = character and character:FindFirstChildOfClass("Humanoid")
                
                local isAlive = humanoid and humanoid.Health > 0
                if character and humanoid and isAlive and player ~= LocalPlayer then
                    local onScreen, pos, size, dist = getSimpleESPBox(character, Camera)
                    
                    if onScreen and pos and size and dist then
                        esp:SetVisible(true)
                        
                        esp.Box.Position = pos
                        esp.Box.Size = size
                        esp.Box.Color = Color3.fromRGB(0, 255, 0)
                        
                        esp.Name.Text = tostring(player.Name) .. " [" .. tostring(math.floor(dist)) .. "]"
                        esp.Name.Color = Color3.fromRGB(255, 255, 255)
                        
                        esp.HealthText.Text = "HP: " .. tostring(math.floor(humanoid.Health))
                        local maxHp = humanoid.MaxHealth or 100
                        local hpPct = math.clamp(humanoid.Health / (maxHp > 0 and maxHp or 100), 0, 1)
                        local r = hpPct < 0.5 and 255 or math.floor(255 - (hpPct - 0.5) * 2 * 255)
                        local g = hpPct < 0.5 and math.floor(hpPct * 2 * 255) or 255
                        esp.HealthText.Color = Color3.fromRGB(r, g, 0)
                        
                        esp.Name.Position = Vector2.new(pos.X + size.X/2, pos.Y - 18)
                        esp.HealthText.Position = Vector2.new(pos.X + size.X/2, pos.Y + size.Y + 2)
                    else
                        esp:SetVisible(false)
                    end
                else
                    esp:SetVisible(false)
                end
            end
        else
            for _, esp in pairs(SimpleESPObjects) do
                esp:SetVisible(false)
            end
        end

        if SilentAimSettings.ESPEnabled then
            for player, esp in pairs(ESPObjects) do
                local character = player.Character
                local humanoid = character and character:FindFirstChildOfClass("Humanoid")
                
                local isAlive = humanoid and humanoid.Health > 0
                if character and humanoid and isAlive and player ~= LocalPlayer then
                    local onScreen, pos, size, dist = getESPBox(character, Camera)
                    
                    if onScreen then
                        esp:SetVisible(true)
                        
                        esp.Box.Color = Color3.fromRGB(220, 220, 220)
                        esp.Name.Color = Color3.fromRGB(220, 220, 220)
                        
                        local hpPct = math.clamp(humanoid.Health / humanoid.MaxHealth, 0, 1)
                        esp.HealthBarFill.Size = Vector2.new(size.X * hpPct, 4)
                        local r = hpPct < 0.5 and 255 or math.floor(255 - (hpPct - 0.5) * 2 * 255)
                        local g = hpPct < 0.5 and math.floor(hpPct * 2 * 255) or 255
                        esp.HealthBarFill.Color = Color3.fromRGB(r, g, 0)
                        
                        esp.HealthText.Text = math.floor(humanoid.Health) .. " / " .. math.floor(humanoid.MaxHealth)
                        
                        local tool = character:FindFirstChildOfClass("Tool")
                        -- esp.EquippedText.Text = tool and tool.Name or "None"
                        
                        for _, part in pairs(character:GetDescendants()) do
                            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                                if part.Material ~= Enum.Material.Neon or part.Color ~= Color3.fromRGB(255, 255, 255) then
                                    part.Material = Enum.Material.Neon
                                    part.Color = Color3.fromRGB(255, 255, 255)
                                end
                                if part:IsA("MeshPart") and part.TextureID ~= "" then
                                    part.TextureID = ""
                                end
                            elseif part:IsA("SpecialMesh") then
                                if part.TextureId ~= "" then
                                    part.TextureId = ""
                                end
                            elseif part:IsA("Decal") or part:IsA("Texture") then
                                if part.Transparency ~= 1 then
                                    part.Transparency = 1
                                end
                            elseif part:IsA("Shirt") or part:IsA("Pants") or part:IsA("ShirtGraphic") or part:IsA("CharacterMesh") then
                                part:Destroy()
                            end
                        end
                        
                        if character:FindFirstChild("HumanoidRootPart") and not character.HumanoidRootPart:FindFirstChild("ESPLight") then
                            local light = Instance.new("PointLight")
                            light.Name = "ESPLight"
                            light.Color = Color3.fromRGB(255, 255, 255)
                            light.Brightness = 3
                            light.Range = 10
                            light.Parent = character.HumanoidRootPart
                        end
                        
                        esp.Box.Position = pos
                        esp.Box.Size = size
                        
                        esp.Name.Text = player.DisplayName .. " [" .. math.floor(dist) .. "m]"
                        esp.Name.Position = Vector2.new(pos.X + size.X/2, pos.Y - 18)
                        
                        esp.HealthBarBg.Position = Vector2.new(pos.X, pos.Y + size.Y + 2)
                        esp.HealthBarBg.Size = Vector2.new(size.X, 4)
                        esp.HealthBarFill.Position = esp.HealthBarBg.Position
                        
                        esp.HealthText.Position = Vector2.new(pos.X + size.X/2, pos.Y + size.Y + 6)
                        esp.EquippedText.Position = Vector2.new(pos.X + size.X/2, pos.Y + size.Y + 20)
                    else
                        esp:SetVisible(false)
                    end
                else
                    esp:SetVisible(false)
                end
            end
        else
            for _, esp in pairs(ESPObjects) do
                esp:SetVisible(false)
            end
        end

        -- Death Backpacks ESP (Independent of ESP type)
        if SilentAimSettings.DeathESPEnabled then
            local deathFolder = workspace:FindFirstChild("DeathBackpacks")
            if deathFolder then
                for _, obj in pairs(deathFolder:GetChildren()) do
                    if not DeathESPObjects[obj] then
                        DeathESPObjects[obj] = createDeathESP()
                    end
                    
                    local esp = DeathESPObjects[obj]
                    local primary = obj:IsA("Model") and (obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart")) or (obj:IsA("BasePart") and obj)
                    
                    if primary then
                        local screenPos, onScreen = Camera:WorldToViewportPoint(primary.Position)
                        local dist = (Camera.CFrame.Position - primary.Position).Magnitude
                        
                        if onScreen and dist <= 100 then
                            local playerBoxHeight = (Camera:WorldToViewportPoint(primary.Position + Vector3.new(0, 3, 0)).Y - Camera:WorldToViewportPoint(primary.Position - Vector3.new(0, 3, 0)).Y)
                            local height = math.abs(playerBoxHeight) * 0.25 -- 1/4 size
                            local width = height * 1.2
                            
                            esp.Box.Visible = true
                            esp.Name.Visible = true
                            esp.DistText.Visible = true
                            
                            esp.Box.Position = Vector2.new(screenPos.X - width/2, screenPos.Y - height/2)
                            esp.Box.Size = Vector2.new(width, height)
                            
                            esp.Name.Position = Vector2.new(screenPos.X, screenPos.Y - height/2 - 14)
                            esp.DistText.Position = Vector2.new(screenPos.X, screenPos.Y + height/2 + 2)
                            esp.DistText.Text = "[" .. math.floor(dist) .. "m]"
                        else
                            esp:SetVisible(false)
                        end
                    else
                        esp:SetVisible(false)
                    end
                end
            end
            
            -- Cleanup removed backpacks
            for obj, esp in pairs(DeathESPObjects) do
                if not obj or not obj.Parent then
                    esp:SetVisible(false)
                    esp:Destroy()
                    DeathESPObjects[obj] = nil
                end
            end
        else
            for _, esp in pairs(DeathESPObjects) do
                esp:SetVisible(false)
            end
        end
        
        if SilentAimSettings.FlyJumpEnabled and flyActive then
            local character = LocalPlayer.Character
            local humanoid = character and character:FindFirstChildOfClass("Humanoid")
            local hrp = character and character:FindFirstChild("HumanoidRootPart")

            if humanoid and hrp and humanoid.Health > 0 then
                local elapsedTime = tick() - flyJumpStart
                local isHoldingJump = (tick() - lastJumpRequest) < 0.2
                
                if isHoldingJump and elapsedTime < 0.5 then
                    hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, 22, hrp.AssemblyLinearVelocity.Z)
                end
                
                if (isHoldingJump or elapsedTime < 0.1) and elapsedTime < 0.5 then
                    FlyJumpContainer.Visible = true
                    local progress = math.clamp(elapsedTime / 0.5, 0, 1)
                    FlyJumpBar.Size = UDim2.new(progress, 0, 1, 0)
                    
                    local r = progress < 0.5 and 255 or math.floor(255 - (progress - 0.5) * 2 * 255)
                    local g = progress < 0.5 and math.floor(progress * 2 * 255) or 255
                    FlyJumpBar.BackgroundColor3 = Color3.fromRGB(r, g, 0)
                    
                    FlyJumpText.Text = string.format("%.1fS", elapsedTime)
                elseif isHoldingJump and elapsedTime >= 0.5 then
                    local params = RaycastParams.new()
                    params.FilterType = Enum.RaycastFilterType.Exclude
                    params.FilterDescendantsInstances = {character}
                    
                    local result = workspace:Raycast(hrp.Position, Vector3.new(0, -9000, 0), params)
                    if result then
                        local hipHeight = humanoid.HipHeight > 0 and humanoid.HipHeight or 2
                        hrp.CFrame = CFrame.new(result.Position + Vector3.new(0, hipHeight + (hrp.Size.Y/2), 0))
                        hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                    end
                    
                    flyActive = false
                    FlyJumpContainer.Visible = false
                else
                    if humanoid.FloorMaterial ~= Enum.Material.Air then
                        flyActive = false
                    end
                    FlyJumpContainer.Visible = false
                end
            else
                flyActive = false
                FlyJumpContainer.Visible = false
            end
        else
            FlyJumpContainer.Visible = false
        end
    end)
end))

local function isHempPrompt(prompt)
    local pName = string.lower(prompt.Name)
    local objText = string.lower(prompt.ObjectText or "")
    local actText = string.lower(prompt.ActionText or "")
    local parentName = prompt.Parent and string.lower(prompt.Parent.Name) or ""
    
    if string.find(pName, "hemp") or string.find(objText, "hemp") or string.find(actText, "hemp") or string.find(parentName, "hemp") then
        return true
    end
    return false
end

local promptsCache = {}

for _, obj in pairs(workspace:GetDescendants()) do
    if obj:IsA("ProximityPrompt") then
        table.insert(promptsCache, obj)
    end
end

workspace.DescendantAdded:Connect(function(obj)
    if obj:IsA("ProximityPrompt") then
        table.insert(promptsCache, obj)
    end
end)

workspace.DescendantRemoving:Connect(function(obj)
    if obj:IsA("ProximityPrompt") then
        for i, p in ipairs(promptsCache) do
            if p == obj then
                table.remove(promptsCache, i)
                break
            end
        end
    end
end)

task.spawn(function()
    while task.wait(0.1) do
        if SilentAimSettings.AutoHempEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local pivot = LocalPlayer.Character.HumanoidRootPart.Position
            
            for _, prompt in ipairs(promptsCache) do
                if prompt and prompt.Parent and prompt.Parent:IsA("BasePart") then
                    local dist = (pivot - prompt.Parent.Position).Magnitude
                    if dist <= (prompt.MaxActivationDistance + 2) then
                        if isHempPrompt(prompt) then
                            if fireproximityprompt then
                                fireproximityprompt(prompt)
                            else
                                prompt:InputHoldBegin()
                                task.wait(prompt.HoldDuration + 0.05)
                                prompt:InputHoldEnd()
                            end
                        end
                    end
                end
            end
        end
    end
end)

local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(...)
    local Method = getnamecallmethod()
    local Arguments = {...}
    local self = Arguments[1]
    local chance = CalculateChance(SilentAimSettings.HitChance)
    if SilentAimSettings.Enabled and self == workspace and not checkcaller() and chance == true then
        
        local function isBulletRaycast(method, args)
            if method == "Raycast" then
                return args[3] and args[3].Magnitude > 15
            elseif method:lower():find("findpartonray") then
                local r = args[2]
                return r and r.Direction and r.Direction.Magnitude > 15
            end
            return false
        end

        -- Check if Method matches and it is a long enough raycast (to avoid hijacking short barrel bounds raycasts causing guns to break)
        if Method:lower() == SilentAimSettings.SilentAimMethod:lower() and isBulletRaycast(Method, Arguments) then
            local HitPart = getClosestPlayer()
            if HitPart then
                local origin
                
                if Method == "FindPartOnRayWithIgnoreList" then
                    if ValidateArguments(Arguments, ExpectedArguments.FindPartOnRayWithIgnoreList) then
                        origin = Arguments[2].Origin
                        Arguments[2] = Ray.new(origin, getDirection(origin, HitPart.Position))
                    end
                elseif Method == "FindPartOnRayWithWhitelist" then
                    if ValidateArguments(Arguments, ExpectedArguments.FindPartOnRayWithWhitelist) then
                        origin = Arguments[2].Origin
                        Arguments[2] = Ray.new(origin, getDirection(origin, HitPart.Position))
                    end
                elseif (Method == "FindPartOnRay" or Method == "findPartOnRay") then
                    if ValidateArguments(Arguments, ExpectedArguments.FindPartOnRay) then
                        origin = Arguments[2].Origin
                        Arguments[2] = Ray.new(origin, getDirection(origin, HitPart.Position))
                    end
                elseif Method == "Raycast" then
                    if ValidateArguments(Arguments, ExpectedArguments.Raycast) then
                        origin = Arguments[2]
                        Arguments[3] = getDirection(origin, HitPart.Position)
                    end
                end

                if origin then
                    task.spawn(function()
                        if SilentAimSettings.ShowBulletTracers then CreateTracer(origin, HitPart.Position) end
                        getgenv().FOVSpeedBoost = 250 -- Tracer triggers speed boost!
                        if SilentAimSettings.ShowHitNotifications then NotifyHit(HitPart.Parent and HitPart.Parent.Name or "Unknown") end
                    end)
                    return oldNamecall(unpack(Arguments))
                end
            end
        end
    end
    return oldNamecall(...)
end))

local oldIndex = nil 
oldIndex = hookmetamethod(game, "__index", newcclosure(function(self, Index)
    if not checkcaller() and SilentAimSettings.DesyncEnabled and Index == "CFrame" and 
       self == (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")) and not isSpinning then
        return desyncT.loc
    end

    if self == Mouse and not checkcaller() and SilentAimSettings.Enabled and SilentAimSettings.SilentAimMethod == "Mouse.Hit/Target" and getClosestPlayer() then
        local HitPart = getClosestPlayer()
         
        if Index == "Target" or Index == "target" then 
            return HitPart
        elseif Index == "Hit" or Index == "hit" then 
            return ((SilentAimSettings.MouseHitPrediction and (HitPart.CFrame + (HitPart.Velocity * SilentAimSettings.MouseHitPredictionAmount))) or (not SilentAimSettings.MouseHitPrediction and HitPart.CFrame))
        elseif Index == "X" or Index == "x" then 
            return self.X 
        elseif Index == "Y" or Index == "y" then 
            return self.Y 
        elseif Index == "UnitRay" then 
            return Ray.new(self.Origin, (self.Hit - self.Origin).Unit)
        end
    end

    return oldIndex(self, Index)
end))


local UIMenuGroup = Tabs["UI Settings"]:AddLeftGroupbox("Menu")

UIMenuGroup:AddToggle("KeybindMenuOpen", {
    Default = Library.KeybindFrame.Visible,
    Text = "Open Keybind Menu",
    Callback = function(value)
        Library.KeybindFrame.Visible = value
    end,
})
UIMenuGroup:AddToggle("ShowCustomCursor", {
    Text = "Custom Cursor",
    Default = true,
    Callback = function(Value)
        Library.ShowCustomCursor = Value
    end,
})
UIMenuGroup:AddDropdown("NotificationSide", {
    Values = { "Left", "Right" },
    Default = 2,
    Text = "Notification Side",
    Callback = function(Value)
        Library:SetNotifySide(Value)
    end,
})
UIMenuGroup:AddDropdown("DPIDropdown", {
    Values = { "50%", "75%", "100%", "125%", "150%", "175%", "200%" },
    Default = 3,
    Text = "DPI Scale",
    Callback = function(Value)
        Value = Value:gsub("%%", "")
        local DPI = tonumber(Value)
        Library:SetDPIScale(DPI)
    end,
})
UIMenuGroup:AddDivider()
UIMenuGroup:AddLabel("Menu bind"):AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "Menu keybind" })

UIMenuGroup:AddButton("Unload", function()
    Library:Unload()
end)

Library.ToggleKeybind = Options.MenuKeybind
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("MobileSilentAim")
SaveManager:SetFolder("MobileSilentAim/config")
SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])
SaveManager:LoadAutoloadConfig()

task.spawn(function()
    while task.wait(0.5) do
        local visible = false
        if SilentAimSettings.BackpackViewEnabled then
            pcall(function()
                local target = GetBestInventoryTarget()
                if target then
                    for _, c in ipairs(backpack_list:GetChildren()) do if c:IsA("ImageLabel") or c:IsA("Frame") then c:Destroy() end end
                    
                    local items = {}
                    -- Equipped item
                    if target.Character then
                        for _, i in ipairs(target.Character:GetChildren()) do
                            if i:IsA("Tool") then table.insert(items, {id = i.TextureId, name = i.Name, using = true}) end
                        end
                    end
                    -- Backpack items
                    local bp = target:FindFirstChild("Backpack")
                    if bp then
                        for _, i in ipairs(bp:GetChildren()) do
                            if i:IsA("Tool") then table.insert(items, {id = i.TextureId, name = i.Name, using = false}) end
                        end
                    end
                    
                    if #items > 0 then
                        visible = true
                        backpack_title.Text = " [" .. target.Name:sub(1,12) .. "] Inventory"
                        for _, item in ipairs(items) do
                            local ic_container = Instance.new("Frame", backpack_list)
                            ic_container.Size = UDim2.new(1, 0, 1, 0)
                            ic_container.BackgroundTransparency = 1
                            
                            if item.id ~= "" then
                                local ic = Instance.new("ImageLabel", ic_container)
                                ic.Size = UDim2.new(1, 0, 1, 0)
                                ic.BackgroundTransparency = 1
                                ic.ScaleType = Enum.ScaleType.Fit
                                ic.Image = item.id
                            else
                                local lbl = Instance.new("TextLabel", ic_container)
                                lbl.Size = UDim2.new(1, 0, 1, 0)
                                lbl.BackgroundTransparency = 1
                                lbl.TextColor3 = Color3.fromRGB(200, 200, 200)
                                lbl.Text = item.name:sub(1, 5)
                                lbl.TextSize = 10
                                lbl.TextWrapped = true
                            end
                            
                            if item.using then
                                local lbl = Instance.new("TextLabel", ic_container)
                                lbl.Size = UDim2.new(1, 0, 0, 15)
                                lbl.Position = UDim2.new(0, 0, 1, -15)
                                lbl.BackgroundColor3 = Color3.fromRGB(0,0,0)
                                lbl.BackgroundTransparency = 0.5
                                lbl.TextColor3 = Color3.fromRGB(255, 50, 50)
                                lbl.Text = "EQUIP"
                                lbl.TextSize = 8
                            end
                        end
                    end
                end
            end)
        end
        backpack_frame.Visible = visible
        local num_items = 0
        for _, c in ipairs(backpack_list:GetChildren()) do if c:IsA("Frame") then num_items = num_items + 1 end end
        backpack_list.CanvasSize = UDim2.new(0, 0, 0, math.ceil(num_items / 4) * 50)
    end
end)

task.spawn(function()
    InitStaffUI()
    while true do
        pcall(function()
            if StaffSettings.Enabled then RefreshStaffList(FetchStaff()) end
        end)
        task.wait(10)
    end
end)

RunService.Heartbeat:Connect(function()
    if not SilentAimSettings.DesyncEnabled or not LocalPlayer.Character then return end
    local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    
    local currentLook = root.CFrame.LookVector
    if prevLookVector then
        local dot = math.clamp(prevLookVector:Dot(currentLook), -1, 1)
        local angleDiff = math.deg(math.acos(dot))
        isSpinning = angleDiff > spinThreshold
    end
    prevLookVector = currentLook
    if isSpinning then return end 
    
    desyncT.loc = root.CFrame
    root.CFrame = desyncT.loc * getOffsetCFrame()
    RunService.RenderStepped:Wait()
    root.CFrame = desyncT.loc
end)
